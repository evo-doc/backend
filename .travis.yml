language: python
python:
  - "3.7"
install:
  - pip install -e .
addons:
  sonarcloud:
    organization: "evodoc" # the key of the org you chose at step #3
    token:
      secure: SONAR_TOKEN
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner